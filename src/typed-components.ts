// my-theme.ts
import { IThemeInterface } from './theme'

const styled: IThemeInterface = {
  borderRadius: '5px',

  colors: {
    main: 'cyan',
    secondary: 'magenta',
  }
};

export default styled;